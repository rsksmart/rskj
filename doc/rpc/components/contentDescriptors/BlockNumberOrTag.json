{
  "BlockNumberOrTag": {
    "name": "blockNumberOrTag",
    "description": "The block to read, given as its height in hex or as a tag",
    "required": true,
    "schema": {
      "$ref": "#/components/schemas/BlockNumberOrTag"
    }
  }
}
