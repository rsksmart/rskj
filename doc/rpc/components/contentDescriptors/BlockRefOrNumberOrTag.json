{
  "BlockRefOrNumberOrTag": {
    "name": "blockRefOrNumberOrTag",
    "description": "The block to read, given as its height in hex, as a tag, or as a block reference object",
    "required": true,
    "schema": {
      "title": "blockRefOrNumberOrTag",
      "oneOf": [
        {
          "$ref": "#/components/schemas/BlockRef"
        },
        {
          "$ref": "#/components/schemas/BlockNumberOrTag"
        }
      ]
    }
  }
}
